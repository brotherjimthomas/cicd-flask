Step 1) Setup AWS resources via console
    * Spin up a small EC2 instance where <user> below is the ami type selected
        ** Ensure that pem file is chmod 400 and the cmd is "ssh -i /path/to/pem <user>@<ip>
        ** Idea for future - terraform this!
    * Jenkins install a bit of a faff from orig, so had to modify using https://www.jenkins.io/doc/book/installing/linux/
        ** Need to update JDK version to at least 21 (ami has v17) found via "journalctl -u jenkins"
            ** This was only needed due to install of v17 manually from further up. 
            "Reverting" via  sudo apt-get install default-jre && sudo apt-get install default-jdk"

Step 4) GitHub config
    * Dockerfile was amazingly easy to draft - probably due to VSCode extension for docker?
    * Let VSCode install the container extension for the docker-compose.yml file

Step 5) Jenkins goodness!
    * Trying fix from https://stackoverflow.com/questions/71043828/stderr-fatal-couldnt-find-remote-ref-refs-heads-master
    This worked but now running into node out of /tmp space (too small)
    * It's /var/lib/jenkins that is causing the issue... 
    Stopped instance. Resized vol to 18G in console. Restarted instance. Seems okay??? Trying pipeline
    Yup - typo in Jenkinsfile for "git url" line
    And found issues with missing files! Didn't have any of the "app stuff". Adding now
        requirements.txt, appy.py, message.sql
    Jenkins bombing out due to OOM. Added swapfile space 2G
    It ran but getting a Jinja template failure. And again, due to missing files (correcting now)
        ../templates/index.html

SUCCESS!    

Final Thoughts
   This was a great exercise to knock the rust off. With the original project being a bit old, it was good 
   to get some troubleshooting due to version updates & whatnot. The push/pipeline/debug loop was helpful 
   and nice to dig into some things.